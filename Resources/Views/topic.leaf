#extend("base"):
  #export("content"):
    
    <h1>Title: #(topic.title)</h1>
    <h2>Description: #(topic.description)</h2>
    <h2>Votes: #count(topic.votes)</h2>
    <h2>Completed: #(topic.completed)</h2>


  <a href="#(topic.id)/votes/create"> Vote on this topic</a>
  <br/>
  #if(topic.completed == false):
    <a href="#(topic.id)/complete"> Mark this topic as complete</a>
  #endif
  #endexport
#endextend
